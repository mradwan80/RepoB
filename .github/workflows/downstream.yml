name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # Allows manual triggering

jobs:
  trigger:
    uses: mradwan80/RepoC/.github/workflows/ci.yml@main
